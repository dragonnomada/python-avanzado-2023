// Proceso de compilación
// Código fuente: *.c
// Archivo compilado (Programa): gcc -o <program> <file>.c
// gcc -o main main.c

// Incluir bibliotecas (headers)
#include <stdio.h>

// Definir un punto de ejecución `main`

// Declarar variables y funciones

// Podemos crear arreglos y punteros

// Todo tipo de dato es explícito
int main() { // Los bloques se limitan por llaves
    printf("Hola mundo\n"); // Todas las sentencias terminan en ;
}